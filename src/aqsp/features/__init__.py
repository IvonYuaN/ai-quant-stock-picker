"""衍生分析特征包。

集中放"本地推演"类的派生特征与**只读标注层**：

- `cyq` / `pit_enrichment`：只用已有 OHLCV + 换手率 / PIT 标签推演，不新增取数依赖。
- `event_calendar`：已知事件（限售解禁、龙虎榜）的**点内安全**查询层。它**自己
  不取数**——记录由调用方预先取好（`scripts/fetch_*.py` 预加载缓存）后注入，
  因此仍然满足「纯计算链路不访问网络」的红线。

本包只产出**标注**，不产出打分权重。
"""
