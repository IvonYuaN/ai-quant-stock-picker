# 最终修正版任务清单

**基于Codex两次验证的准确清单**

---

## ✅ Codex已完成并验证的修复

### 1. 核心性能热点优化
- ✅ `src/aqsp/data/cache.py` - 批量写库（iterrows → executemany + itertuples）
- ✅ `src/aqsp/data/sqlite_db_source.py` - 符号映射加载优化
- ✅ `src/aqsp/strategy.py` - 防御式数据校验

### 2. 测试验证
- ✅ `pytest tests/test_strategy.py tests/test_data_cache.py tests/test_data_validation.py tests/test_data_source.py -q`
- ✅ 结果：71 passed
- ✅ `ruff check` 和 `ruff format --check` 全通过

---

## ❌ Claude的残留错误（已再次修正）

### 错误1: midday.py不存在
**Claude说**: "修复 `src/aqsp/briefing/midday.py` 的空值问题"  
**真相**: 这个文件不存在，是Claude臆造的  
**实际情况**: 只有intraday流程和dashboard_beginner.py中的午盘导航

### 错误2: Docstring判断不准确
**Claude说**: "screen_universe()、filter_t1_held() 缺少docstring"  
**真相**: 
- ✅ `strategy.py:18` 的 `screen_universe()` **已有** docstring
- ✅ `t1_filter.py:42` 的 `filter_t1_held()` **已有**（虽然简短）
- ❌ `briefing/generator.py:530` 的 `generate()` **真正缺少**

### 错误3: 新手术语表状态错误
**Claude说**: "已优化"  
**真相**: `dashboard_beginner.py` 中**没有** GLOSSARY 或术语解释块  
**状态**: 待做任务，不是已完成

---

## 📋 准确的待办任务

### P1 - 建议修复（工时约4小时）

#### 任务1: 补充 generator.generate() 的Docstring
**位置**: `src/aqsp/briefing/generator.py:530`

**当前状态**: 缺少docstring  
**工时**: 30分钟

**建议内容**:
```python
def generate(
    self,
    picks: Sequence[PickResult],
    screen_frames: dict[str, pd.DataFrame],
    regime: MarketRegime | None = None,
    sector_concentration: SectorConcentration | None = None,
    correlation_result: CorrelationResult | None = None,
) -> Briefing:
    """
    生成每日量化选股简报。
    
    Args:
        picks: 候选股票列表（已排序）
        screen_frames: 股票代码到OHLCV数据的映射
        regime: 市场状态识别结果（可选）
        sector_concentration: 行业集中度分析（可选）
        correlation_result: 股票相关性分析（可选）
    
    Returns:
        Briefing对象，包含：
        - title: 简报标题
        - subtitle: 副标题
        - sections: 各个内容板块
        - summary: 一句话总结
    
    Example:
        >>> generator = BriefingGenerator()
        >>> briefing = generator.generate(picks, frames, regime)
        >>> print(briefing.to_markdown())
    """
```

---

#### 任务2: 新手仪表盘添加术语表
**位置**: `src/aqsp/web/dashboard_beginner.py`

**当前状态**: 没有术语解释  
**工时**: 1小时

**具体实现**:
```python
# 在文件开头定义
BEGINNER_GLOSSARY = {
    "bias20": "价格与20日均线的偏离度（%）\n偏高→可能回调，偏低→可能反弹",
    "rps": "相对强度排名（0-100）\n数字越高，相对其他股票涨得越好",
    "ma_full_bull": "均线多头排列\n短期均线在长期均线上方，表示上升趋势",
    "均线缩量回踩": "价格小幅回落但成交量减少\n可能是主力洗盘，不是真跌",
    "N字反弹": "价格先跌后涨形成N字形\n突破前高可能继续上涨",
    "突破平台": "股价突破长期横盘区间\n可能开启新一轮涨势",
    "止损线": "能接受的最大亏损位置\n跌破这个价格就卖出，防止亏更多",
    "T+1": "今天买的股票，今天不能卖\n要等明天（T+1）才能卖",
}

# 在 tab4 新手教程标签中添加
with tab4:
    st.markdown("### 📚 看不懂这些词？")
    
    # 分类显示
    st.markdown("#### 技术指标")
    for term in ["bias20", "rps", "ma_full_bull"]:
        with st.expander(f"❓ {term}"):
            st.markdown(BEGINNER_GLOSSARY[term])
    
    st.markdown("#### 形态描述")
    for term in ["均线缩量回踩", "N字反弹", "突破平台"]:
        with st.expander(f"❓ {term}"):
            st.markdown(BEGINNER_GLOSSARY[term])
    
    st.markdown("#### 交易规则")
    for term in ["止损线", "T+1"]:
        with st.expander(f"❓ {term}"):
            st.markdown(BEGINNER_GLOSSARY[term])
```

---

#### 任务3: 优化剩余iterrows（非关键路径）
**位置**:
- `src/aqsp/ledger/base.py:370`
- `src/aqsp/ledger/learner.py:53, 437`
- `src/aqsp/backtest/walk_forward.py:614, 637`
- `src/aqsp/filters_lethal/lockup_release.py`
- `src/aqsp/strategies/intraday_trade.py`
- `src/aqsp/research_engine.py`
- `src/aqsp/web/dashboard_beginner.py`

**优先级**: P1（后续优化队列，非紧急）  
**工时**: 2-3小时

**通用修复模式**:
```python
# Before (慢)
for _, row in df.iterrows():
    results.append(process(row['col1'], row['col2']))

# After (快3-10倍)
for row in df.itertuples(index=False):
    results.append(process(row.col1, row.col2))
```

---

### P2 - 后续设计（需要架构讨论）

#### 任务4: 设计持仓快照契约
**当前状态**: T+1过滤和止损计算已生效，但缺少统一的持仓状态抽象

**不是修复，是架构设计**:

1. 定义只读持仓快照协议
2. 让ledger实现快照提供者
3. 风控模块消费快照

**工时**: 4-8小时（需要讨论设计）

**注意**: 
- ❌ 不要硬接现有的 PositionTracker（构造函数签名不匹配）
- ❌ 不要在 manager.py 中假集成
- ✅ 保持当前"上游显式过滤后进入PM"的设计

---

### P3 - 可选优化（有空再做）

#### 任务5: 拆分cli.py（4365行）
**工时**: 8小时

#### 任务6: 创建故障排查文档
**工时**: 2小时

#### 任务7: 固定依赖版本范围
**工时**: 15分钟

---

## 📊 当前项目真实评分

**总体评分: 7.5/10**（Codex修复后）

| 维度 | 评分 | 说明 |
|------|------|------|
| 代码质量 | 8/10 | ✅ 核心性能已优化 |
| 集成完整度 | 7/10 | ✅ T+1和止损已生效 |
| 新手友好度 | 7/10 | ⚠️ 术语表待添加 |
| 生产就绪度 | 7.5/10 | ✅ 可用于中小规模 |
| 文档完整度 | 6/10 | ⚠️ 部分docstring缺失 |

### 关键事实（已验证）

- ✅ T+1过滤生效（cli.py:2399 调用 filter_t1_held）
- ✅ 止损计算生效（cli.py:2516 调用 compute_dynamic_stop）
- ✅ 数据验证完整（fetcher.py:135 使用 DataValidator）
- ✅ 核心性能热点已修复（cache.py, sqlite_db_source.py）
- ✅ 测试全部通过（71 passed）
- ⚠️ 新手术语表待添加
- ⚠️ generator.generate() 缺docstring

---

## 🎯 完成P1后预期评分

**8.0/10**

| 维度 | 当前 | 完成后 | 提升 |
|------|------|--------|------|
| 代码质量 | 8/10 | **8.5/10** | +0.5 |
| 新手友好度 | 7/10 | **8/10** | +1 |
| 文档完整度 | 6/10 | **7.5/10** | +1.5 |

---

## 🚀 给Codex的准确指令

```
@codex 请执行以下3个P1任务：

1. 补充 src/aqsp/briefing/generator.py:530 的 generate() 方法docstring
   - 参考我提供的模板
   - 包含参数说明、返回值、示例

2. 在 src/aqsp/web/dashboard_beginner.py 添加新手术语表
   - 定义GLOSSARY字典（8-10个常见术语）
   - 在tab4教程标签中添加expander展示
   - 分类：技术指标、形态描述、交易规则

3. 优化 ledger/base.py 和 backtest/walk_forward.py 中的iterrows
   - 改用 itertuples(index=False)
   - 保持原有逻辑不变
   - 运行相关测试确认无回归

不要尝试修复不存在的 midday.py 文件。
不要硬接 PositionTracker 到 cli.py（参数不匹配）。
```

---

## 📝 修正记录

### 第1次（Claude误判）
- ❌ 认为T+1完全未集成
- ❌ 认为StopLossManager完全未集成
- ❌ 建议的PositionTracker集成代码有误
- ❌ 15处iterrows都标为P0

### 第2次（Codex纠正后仍有错）
- ❌ 臆造了不存在的 midday.py
- ❌ 误判screen_universe()缺docstring（实际有）
- ❌ 误判新手术语表为"已完成"（实际未做）

### 第3次（本次修正）
- ✅ 基于grep实际验证
- ✅ 只列出真正缺失的功能
- ✅ 优先级准确
- ✅ 状态标注准确

---

**本清单基于实际代码验证，Codex两次核查确认。**
