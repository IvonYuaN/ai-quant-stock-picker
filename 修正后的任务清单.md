# 修正后的优化任务清单

**基于实际代码审查，Codex已修正Claude的误判**

---

## Codex已完成的修复 ✅

1. ✅ **cache.py性能热点** - iterrows改为executemany + itertuples
2. ✅ **sqlite_db_source.py启动优化** - iterrows改为itertuples  
3. ✅ **strategy.py防御式校验** - 空frame和坏数据过滤
4. ✅ **测试验证通过** - test_strategy.py, test_data_cache.py

---

## Claude的错误判断（已被Codex纠正）

### ❌ 错误1: "T+1完全未集成"
**真相**: 
- `src/aqsp/cli.py` line 2399-2409 已调用 `filter_t1_held()`
- `src/aqsp/universe/t1_filter.py` line 42 基于昨日ledger剔除标的
- **准确说法**: T+1过滤已生效，但缺少完整持仓状态引擎

### ❌ 错误2: "PositionTracker未集成"建议错误
**真相**:
- `position_tracker.py` line 178 的构造函数只有 `positions` 字段
- **没有** `ledger_path`、`paper_trades_path` 参数
- Claude建议的集成代码会直接报错

### ❌ 错误3: "StopLossManager完全未集成"  
**真相**:
- `src/aqsp/cli.py` line 2516-2523 已调用 `compute_dynamic_stop()`
- 会真实更新 `pick.stop_loss`
- `src/aqsp/portfolio/manager.py` line 311 明确注释："没有真实持仓快照前，不做假集成"
- **准确说法**: 止损计算已生效，但StopLossManager没进主链是设计选择

### ❌ 错误4: "DataValidator使用不完整"过度判断
**真相**:
- `src/aqsp/data/fetcher.py` line 135-151 已完整使用 `DataValidator`
- 无效symbol被丢弃，全部无效会 `raise DataError`
- **准确说法**: fetcher已有验证，Codex补充了screen_universe边界校验

### ❌ 错误5: "15处iterrows都是P0"优先级错误
**真相**:
- 真正的热点是 `cache.py` 和 `sqlite_db_source.py`（已修复）
- 其他位置在脚本、示例、非关键路径
- **准确说法**: 核心热点已优化，其他位置非紧急

---

## 真实的剩余问题

### P1 - 重要优化（建议修复）

#### 问题1: 剩余的iterrows在非热点路径
**位置**: 
- `src/aqsp/ledger/base.py:370`
- `src/aqsp/ledger/learner.py:53, 437`
- `src/aqsp/backtest/walk_forward.py:614, 637`
- `src/aqsp/filters_lethal/lockup_release.py`
- `src/aqsp/strategies/intraday_trade.py`
- `src/aqsp/research_engine.py`

**优先级**: P1（非紧急，但长期值得改）

**修复建议**:
```python
# 通用模式
# Before
for _, row in df.iterrows():
    process(row['col1'], row['col2'])

# After (快3-10倍)
for row in df.itertuples(index=False):
    process(row.col1, row.col2)
```

---

#### 问题2: 缺少完整持仓状态引擎

**现状**:
- ✅ T+1过滤已生效（基于ledger）
- ✅ 止损计算已生效（dynamic_stop）
- ❌ 但缺少统一的持仓快照契约

**建议**:
不要硬接现有的 `PositionTracker` 到cli.py，而是：

1. **定义只读持仓快照契约**:
```python
# 新建 src/aqsp/portfolio/snapshot.py
@dataclass
class PositionSnapshot:
    """只读持仓快照（研究链路使用）"""
    symbol: str
    shares: int
    cost_basis: float
    frozen_shares: int  # T+1冻结
    stop_loss_price: float
    current_price: float
    
class SnapshotProvider(Protocol):
    """持仓快照提供者契约"""
    def get_snapshot(self, date: date) -> list[PositionSnapshot]:
        ...
```

2. **让ledger实现快照提供者**:
```python
# 修改 src/aqsp/ledger/base.py
class PaperLedger(SnapshotProvider):
    def get_snapshot(self, date: date) -> list[PositionSnapshot]:
        """从ledger重建当日持仓快照"""
        # 基于历史记录计算持仓
        ...
```

3. **风控模块消费快照**:
```python
# 在 cli.py 中
snapshot = ledger.get_snapshot(today)
# 传给止损检查、T+1验证等
```

**不要做**:
- ❌ 直接new PositionTracker()然后传不存在的参数
- ❌ 把研究链路当实盘引擎
- ❌ 在manager.py中假集成

---

#### 问题3: 午盘分析缺少空值保护

**位置**: `src/aqsp/briefing/midday.py`

**问题**: 如果prices为空会崩溃

**修复**:
```python
def analyze_morning_performance(picks, prices):
    if not prices:
        return {
            "picks": [],
            "avg_change": 0.0,
            "warning": "⚠️ 无法获取实时价格数据"
        }
    # 继续原逻辑
```

---

#### 问题4: 核心函数缺少Docstring

**位置**:
- `src/aqsp/strategy.py` - screen_universe()
- `src/aqsp/briefing/generator.py` - generate()
- `src/aqsp/universe/t1_filter.py` - filter_t1_held()

**建议**: 补充文档，尤其是参数和返回值说明

---

### P2 - 体验优化（可选）

#### 优化1: cli.py过大（4365行）

**建议拆分**:
```
src/aqsp/cli/
├── __init__.py
├── commands.py      # 命令注册
├── run.py           # run命令
├── midday.py        # midday命令
└── utils.py         # 公共逻辑
```

**优先级**: P2（不紧急，但有助维护）

---

#### 优化2: 新手仪表盘添加术语解释

**位置**: `src/aqsp/web/dashboard_beginner.py`

**添加**:
```python
GLOSSARY = {
    "bias20": "价格偏离20日均线的百分比",
    "rps": "相对强度排名（越高越强）",
    "均线缩量回踩": "小幅回调但成交量减少",
    # ...
}

# 在教程标签添加术语表
```

---

#### 优化3: 故障排查文档

**创建**: `docs/TROUBLESHOOTING.md`

**包含**:
- 锁文件残留处理
- 选股结果为空排查
- 通知未收到排查
- 仪表盘启动失败

---

#### 优化4: 依赖版本固定范围

**位置**: `pyproject.toml`

**建议**:
```toml
dependencies = [
    "pandas>=2.0,<3.0",
    "numpy>=1.24,<2.0",
    # 防止breaking changes
]
```

---

## 执行优先级

### 立即可做（P1 - 本周）

1. **补充Docstring**（3小时）
   - screen_universe()
   - generate()
   - filter_t1_held()

2. **午盘空值保护**（30分钟）
   - analyze_morning_performance()

3. **新手术语表**（1小时）
   - dashboard_beginner.py

### 长期改进（P1 - 本月）

4. **设计持仓快照契约**（4小时）
   - 定义SnapshotProvider协议
   - ledger实现快照
   - 不要硬接现有PositionTracker

5. **优化剩余iterrows**（2小时）
   - ledger/base.py
   - backtest/walk_forward.py

### 可选优化（P2 - 有时间再做）

6. **拆分cli.py**（8小时）
7. **创建故障排查文档**（2小时）
8. **固定依赖版本**（15分钟）

---

## 当前项目真实评分

**总体评分: 7.5/10**（Codex修复后）

| 维度 | 评分 | 说明 |
|------|------|------|
| 代码质量 | 8/10 | 核心性能已优化 ✅ |
| 集成完整度 | 7/10 | T+1、止损已生效，缺统一持仓引擎 |
| 新手友好度 | 7/10 | 通知和仪表盘已优化 |
| 生产就绪度 | 7.5/10 | 可用于中小规模测试 |

### 关键事实

- ✅ **T+1已生效** - filter_t1_held()在主链
- ✅ **止损已生效** - compute_dynamic_stop()更新stop_loss
- ✅ **数据验证已生效** - DataValidator在fetcher.py
- ✅ **性能热点已优化** - cache.py和sqlite已修复
- ⚠️ **缺少统一持仓引擎** - 需要设计而非硬接

---

## 完成P1后预期评分

**8.5/10**

| 维度 | 当前 | 完成后 | 提升 |
|------|------|--------|------|
| 代码质量 | 8/10 | **9/10** | +1 |
| 集成完整度 | 7/10 | **8.5/10** | +1.5 |
| 新手友好度 | 7/10 | **8/10** | +1 |
| 文档完整度 | 6/10 | **8/10** | +2 |

---

## 总结

### Codex的修正是正确的

1. ✅ T+1已集成，不是"完全缺失"
2. ✅ 止损计算已生效，不是"完全未集成"
3. ✅ 数据验证已完整，不是"使用不完整"
4. ✅ 核心性能热点已修复
5. ✅ Claude的集成建议会写错（参数不存在）

### 真正需要做的

1. **补充文档**（最重要）
2. **设计持仓快照契约**（而非硬接PositionTracker）
3. **优化剩余iterrows**（非紧急）
4. **小的体验优化**（可选）

### 不要做的

- ❌ 硬接PositionTracker到cli.py（参数不存在）
- ❌ 硬接StopLossManager（架构不支持）
- ❌ 认为T+1/止损完全缺失（已经在工作）

---

**本清单基于实际代码验证，Codex的修正是准确的。**
