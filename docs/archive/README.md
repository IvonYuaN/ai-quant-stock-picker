# Archive Index

这里存放“保留追溯价值，但不再作为当前执行入口”的材料。

## `experiments/`

walk-forward 试验、数据源切换验证、参数诊断、阶段性失败记录。

## `process/`

PR 拆分、审查记录、排障过程、临时执行说明。

归档原则：

- 当前仍会被日常操作依赖的文档，留在 `docs/`
- 只反映某个时间点试验或协作过程的文档，移到 `docs/archive/`
- 新增归档文件时，不要再把它们挂回根级 README，当作历史材料保存即可
